aws-cli
=======

CLI for the AWS API

Installation
------------

*Requirements*

Create a `.boto` file in your home directory: http://boto.readthedocs.org/en/latest/boto_config_tut.html

    pip install cement
    pip install boto
